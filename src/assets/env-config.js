(function (window) {
  window.__env = window.__env || {};
  window.__env.ENV = window.__env.ENV || "local";
  // TODO window.__env.PAQ1_URL = window.__env.EPOCHLITHE_URL || "https://epochlithe.dev.doggoverse.fr"; // "http://localhost:9000";
})(this);
